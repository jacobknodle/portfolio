# The differentiation engine itself has NO third-party dependencies and runs
# on the Python standard library alone (see server.py and cli.py).
#
# Flask is only needed if you want to run the production-style API in app.py.
Flask>=3.0,<4.0
